<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>

</head>
<body>
   
    <div class="main w-full min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white" >
            <form action="/create" method="post">
                <input class="bg-zinc-800 block py-5 px-3 w-full rounded-2xl "  type="text" placeholder="Title" name="title">
                <textarea class="bg-zinc-800 block mt-2 py-5 px-3 resize-none w-full rounded-2xl " placeholder="Write your tasks Details" name="textarea"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-2xl" type="submit" placeholder="Submit Task">
            </form>
        </div>

        <div class="tasks p-10  flex gap-3 flex-wrap">

            <% if(files.length > 0){%>
                <% files.forEach((val) => { %>
                 
                    <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-2xl "> 
                        <h1 class=" text-white text-3xl tracking-tighter"><%= val %></h1>
                        <div class=" w-full flex justify-between items-center">
                        <a class="text-blue-500 inline-block " href="/file/<%= val %>">Read More</a>
                        <a class="text-zinc-500" href="/edit/<%= val %>">edit</a>
                    </div>
                    </div>

               <% }) } else { %>
                <h3 class="text-zinc-500">No Task yet....</h3>

              <% } %>
            

        </div>
    </div>
</body>
</html>